---
"canvist_core": patch
---

Improve crate READMEs and add license files for publishing

### crate READMEs

- `canvist_core/readme.md` — expanded with features list, quick start example
  (runtime + formatting + undo), collaboration example, and export examples.
- `canvist_render/readme.md` — expanded with trait overview, backend
  implementation example, and component list.

### Publishing readiness

- Added `license` file to each crate directory for crate packaging.
- Updated workspace `include` to include `license` in published packages.
